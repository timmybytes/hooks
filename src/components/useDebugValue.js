import React, { useDebug } from 'react';
import HookInfo from './HookInfo';

const UseDebugValueComponent = () => {
  //
  return <HookInfo name='useDebugValue' />;
};

export default UseDebugValueComponent;
